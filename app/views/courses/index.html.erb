<div class='container'>
  <ul class="collapsible" data-collapsible="accordion">
    <% @courses.each do |course| %>
      <li>
        <div class="collapsible-header rw"><i class="material-icons">filter_drama</i>
          <%= course.name %>
        </div>
        <div class="collapsible-body">
          <p>Course description: ...</p>
        </div>
      </li>
    <% end %>
  </ul>
</div>
